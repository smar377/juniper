# Copy this file to lab.env, fill in your values, then source it before running scripts:
#   cp lab.env.example lab.env && source lab.env

export JUNOS_HOST=172.25.11.1
export JUNOS_USER=brook
export JUNOS_PASSWD=

# Used by junos_add_users.py — password for the new user being provisioned
export NEW_USER_PASSWD=
